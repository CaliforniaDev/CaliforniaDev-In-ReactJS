import { StyledSpan } from './StateLayer.styles';

// StateLayer Component: A layer that adjusts opacity based on interaction states.

export const StateLayer = ({ className }) => {
  return <StyledSpan className={className}></StyledSpan>;
};
